
kubectl create secret docker-registry docker-registry-cred --docker-username=admin --docker-password=P@ssw0rd --docker-server=string
kubectl create secret generic docker-registry-cred --from-file=.dockerconfigjson=/root/.docker/config.json --type=kubernetes.io/dockerconfigjson